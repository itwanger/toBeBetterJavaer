---
title: 阿里员工：在 Qwen 事业部一个月，周末来公司健身灯都是关的，基本没人加班。最爽的是正式员工每月 200 刀 AI 额度，能感受到踩中 AI 红利的轻松氛围了（附Agent面试题）
shortTitle: 阿里Qwen事业部Agent面试10问
description: 阿里 Qwen 事业部员工分享工作氛围与 AI 红利，附 10 道 Agent 面试题全解：ReAct 执行流程、Tool Message 设计、记忆系统、上下文压缩、多模态融合、Agent 训练、Redis 工程化。
keywords: Agent 面试题, ReAct, Tool Message, 上下文压缩, Redis Agent
tag:
  - 面试
  - Agent
category:
  - AI
author: 沉默王二
date: 2026-08-26
---

如题，看到有同学在阿里 Qwen 事业部实习了一个月，分享了一些和华子文化、工作强度、氛围的差异。

我简单帮大家总结一下。

华子周末双倍工资但基本要加班，阿里周末来公司健身灯都是关的，基本没人加班。平常九点半大家就走了，七点新员工就溜得差不多了。最爽的是正式员工每月 200 刀 AI 额度，虽然实习生没有，但能感受到踩中 AI 红利的轻松氛围了。

![截图来自某书](https://cdn.paicoding.com/stutymore/sucai-20260826103042.png)

这条帖子里有个细节很有意思——每月 200 刀 AI 额度。折合人民币差不多 1400 块，这个价格够一个开发者把 Claude Code 重度用一个月了。公司愿意给到这个预算，说明 Qwen 事业部的同学在日常工作中高频使用 AI 工具，而且公司认可这个投入产出比。

一个团队把 AI 工具当作水电一样的基础设施来用，这个团队的工程效率大概率不会差。

评论区还有一些同学提供的信息，我也一并整理了下，大家感受下。

同学A：阿里的假期是最好的福利，P7 员工，一年 20 天到 25 天假，入职 10 天年假起加 7 天陪伴假加 3 天路途假。

这真的爽啊，我上周去三亚就整整 10 天，感觉挣钱的动力又足了，哈哈。说句实在话，休息好了才有心思学新东西。我自己就是这样，工作节奏一紧，回到家就只想瘫着，什么 Agent 什么新模型都提不起兴趣。反而是在不那么卷的状态下，晚上回家还有精力翻翻 GitHub、试试新工具。

同学B：阿里实习生有 Qoder，上周以前也是随便用的，有实习生用了 30w 的额度（约一万元），现在额度用太高需要财务审批了。我个人是用 Qoder 作为 Claude Code 的备胎。

30 万的额度是个什么概念？Claude Code 重度使用一天差不多 5 到 10 美元，30 万人民币折合大约 4 万美元，相当于不间断地用上一整年。这位同学要么是在疯狂出活，要么是找到了什么特别烧 token 的用法。不管哪种，公司愿意让实习生随便用 AI Coding 工具，说明在阿里内部这已经是标配了。

借此机会，我再给大家梳理一下阿里的 AI 护城河业务。

![](https://cdn.paicoding.com/stutymore/sucai-20260826105649.png)

千问模型决定阿里 AI 的能力上限，阿里云提供算力和商业化能力，百炼、Qoder、千问办公负责把模型包装成可以工作的 Agent，淘宝天猫、淘宝闪购、高德、菜鸟等业务，则提供真实的应用场景。

我个人是非常看好阿里的。模型、算力、工具、场景——这四块能自己串起来的国内公司没几家。千问模型发展得一直很快，Qoder 作为阿里的 AI Coding 工具也在持续迭代。对我们开发者来说，做 Agent 开发的时候选型上多了不少靠谱选项，千问的 Embedding 模型、百炼平台的 API，都是拿来就能用的。

如果你是一位愿意相信努力、相信过程、相信一步一个脚印、相信自己能在 AI 时代分一杯羹的人，那接下来的硬核内容，希望你能认真读一读。

![](https://cdn.paicoding.com/stutymore/sucai-20260824103109.png)

（全文比较肝，保证大家能学到很多很多，系好安全带，我们粗粗发～）

## content

### 01、自我介绍一下，重点说 Agent 相关的项目经验

我做过三个和 Agent 相关的项目。

【截图：三个Agent项目定位图；风格：three-layer；截图目标：展示三个项目的定位和技术栈差异；关键词：PaiCLI、PaiAgent、派聪明】

第一个是 PaiCLI，一个 Java 版的终端 AI Agent，对标 Claude Code。开发者在终端里用自然语言和 Agent 交互，完成代码编写、调试、搜索、文件操作等任务。支持 ReAct、Plan-and-Execute、Multi-Agent Team 三种执行模式，底层用的是 Spring AI。我负责 Prompt 组装、上下文压缩、Skills 体系设计和效果评估。

第二个是 PaiAgent，一个 AI 工作流编排平台。用户通过可视化界面拖拽节点来编排 Agent 工作流，底层有两个引擎可以切换——自研的 DAG 引擎做拓扑排序执行，LangGraph4j 做更复杂的条件分支和状态管理。我负责节点执行器的设计和跨节点的状态传递。

第三个是派聪明，一个基于 ES 混合检索的 RAG 知识库。检索用的是千问的 text-embedding-v4 做向量化，KNN 召回加 BM25 重排序，支持三级权限隔离。Agent 对话模块用 DeepSeek 跑 ReAct 循环，最多 4 轮，工具包括知识检索、主题摘要、用户反馈收集。

### 02、介绍一个你比较熟悉的 Agent 项目

重点说说 PaiCLI。

业务背景是这样的：开发者需要一个在终端里就能用的 AI 助手，不用切到浏览器或者 IDE 插件。输入一句话，Agent 自己判断该读文件、搜代码、跑命令还是直接回答。

【截图：PaiCLI 三条执行路径架构图；风格：swimlane；截图目标：展示三种模式的触发条件和执行流程差异；关键词：ReAct、Plan、Team 模式】

架构上设计了三条执行路径。ReAct 模式是默认路径，单循环——LLM 决策、工具调用、观察结果、再决策，适合实时交互的短任务。Plan-and-Execute 模式用于复杂任务，先让规划器生成一个带依赖关系的任务图，用拓扑排序确定执行顺序，中途失败会触发重新规划。Team 模式是多 Agent 协作，规划器拆任务、Worker 并行干活、审查器检查质量。

我主要负责四块工作。

第一块是 9 层 Prompt 分层组装，静态层（身份、人格、模式、审批）排在最前面命中 Prompt Caching，动态层（运行时上下文、项目记忆、Skills 索引）每轮更新。

【截图：9 层 Prompt 分层结构；风格：three-layer；截图目标：展示静态层和动态层的分布及 Prompt Caching 命中原理；关键词：静态层、动态层、Prompt Caching】

第二块是两层上下文压缩，短期记忆用 Map-Reduce 做摘要，对话历史在接近窗口上限时整体压缩，保留最近 3 轮完整交互。第三块是 Skills 渐进式加载，索引层只占 4KB 常驻上下文，正文按需加载，LRU 缓冲区最多同时持有 3 个 Skill。第四块是三层效果评估，维护了 123 条确定性测试用例的 Golden Set，结合 LLM-as-Judge 和人工抽检做回归测试。

### 03、项目中采用了单 Agent、多 Agent 还是 Workflow？

PaiCLI 采用的是分层设计，三种模式并存，根据任务复杂度选择。

默认走单 Agent，也就是 ReAct 模式。大部分用户的输入是短任务——“帮我改一下这个函数”“搜一下这个报错”，一个 ReAct 循环几步就能搞定。

【截图：三种模式适用场景对比；风格：data-board；截图目标：展示单 Agent/多 Agent/Workflow 的适用场景和开销差异；关键词：任务复杂度、通信成本、执行效率】

复杂任务切到 Plan-and-Execute。用户说“帮我重构这个模块的所有测试用例”，涉及多文件多步骤，单循环容易跑偏。规划器先生成一个带依赖关系的 JSON 计划，按拓扑排序分批执行，每批执行完后可以重新规划。

协作任务切 Team 模式。规划器只负责拆任务，Worker 并行干活（默认最多 2 个 Worker 同时工作），审查器检查质量——不合格打回重做，最多 2 次。规划器和审查器都不碰工具，只做判断，工具执行权集中在 Worker 手里。

PaiAgent 和 PaiFlow 则走 Workflow 编排路线。用户通过可视化界面拖拽节点组装流程，DAG 引擎用 Kahn 算法做拓扑排序，按依赖关系分批执行节点。LangGraph4j 引擎支持条件分支和循环，适合更复杂的流程控制。

#### 为什么不直接上多 Agent？

多 Agent 的通信成本高。每个 Agent 维护独立的对话历史，模型调用次数成倍增长。一个简单的“改个变量名”任务，单 Agent 一轮就完事，多 Agent 要走规划、分派、执行、审查几个来回，token 消耗差好几倍。

【截图：单 Agent 和多 Agent 的 token 消耗对比；风格：data-board；截图目标：展示简单任务下两种模式的开销差距；关键词：模型调用次数、token 消耗、任务复杂度】

分层设计的好处是按需升级——简单任务用最轻量的方式完成，只有当任务复杂度真的需要多角色协作时才启动 Team 模式。

### 04、ReAct Agent 的核心执行流程是什么？

ReAct 的循环分三个环节：Thought、Action、Observation。

Thought 是模型的思考过程——当前任务是什么、已经收集到哪些信息、下一步应该做什么。这个环节不产生可见的动作，但它决定了后续调用哪个工具、传什么参数。

Action 是模型做出的决策，两种结果：调用工具（tool_call），或者给出最终答案（final_answer）。

Observation 是工具返回的结果。搜索工具返回匹配的代码片段，文件读取工具返回文件内容，命令执行工具返回终端输出。这些结果注回上下文，模型基于新信息重新进入 Thought 环节。

【截图：ReAct 循环流程图；风格：swimlane；截图目标：展示 Thought → Action → Observation 的循环和退出条件；关键词：ReAct 循环、tool_call、final_answer】

PaiAgent 的 ReAct 实现用 JSON 格式做决策：

```json
{"action":"tool_call","reasoningSummary":"需要搜索相关代码","toolName":"search_code","toolInput":{"query":"用户登录"}}
{"action":"final_answer","reasoningSummary":"已找到所有相关文件","finalAnswer":"共找到3个文件..."}
```

最大步数默认 5 步，可配置到 20 步。这个上限是必须设的——模型有时候会陷入循环，工具报错就换个参数重试，还是报错就再换，没有步数限制就会一直跑下去。每一步的执行轨迹（输入、输出、状态、耗时）都完整记录，排查问题时可以回溯到具体哪一步出了问题。

#### Observation 为什么要注回上下文？

工具返回的信息是模型做下一步决策的唯一依据。

【截图：Observation 注回上下文的数据流；风格：whiteboard；截图目标：展示工具返回结果如何参与下一轮 Thought 决策；关键词：工具返回、上下文注入、决策依据】

比如 Agent 需要修改一个 bug，第一步调了搜索工具找到了报错所在的文件。如果搜索结果不注回上下文，模型根本不知道报错在哪个文件、第几行、什么内容，后续的修复就成了盲猜。注回之后，模型的下一轮 Thought 能看到完整的搜索结果，做出有依据的决策。

这就是 ReAct 和纯规划类 Agent 的区别——每一步行动都有观察反馈，模型是在“看到实际情况”之后再决定怎么做。

### 05、Agent 调用工具后，Tool Message 怎么设计？

LLM 对话协议里有三种消息角色：user（用户输入）、assistant（模型输出）、tool（工具返回）。

为什么要给工具返回单独设一个角色？因为工具返回的内容在性质上和用户输入、模型输出都不一样。

【截图：三种消息角色对比；风格：data-board；截图目标：展示 user/assistant/tool 三种角色的职责和区分必要性；关键词：消息角色、tool_call_id、并行调用】

把工具结果塞进 user 消息，模型会以为是用户在说话——“找到 3 个匹配文件”变成了用户的需求描述，模型可能会把它当作新任务来处理。塞进 assistant 消息，模型会以为这是自己之前说过的话，后续推理可能在这个基础上继续编造。独立的 tool 角色让模型明确知道：这是外部工具返回的客观结果。

消息结构的关键字段是 tool_call_id。一次 LLM 响应可以同时调用多个工具（并行工具调用），每个调用都有一个唯一 ID。工具执行完后，通过 ID 把结果和对应的调用请求一一绑定。

```json
{
  "role": "tool",
  "tool_call_id": "call_abc123",
  "name": "search_code",
  "content": "找到 3 个匹配文件：UserService.java、AuthController.java、LoginTest.java"
}
```

PaiCLI 的工具执行支持并行——多个工具没有依赖关系时，线程池并发执行，上限 4 个，批量超时 90 秒。结果按原始调用顺序返回，每条结果都以 tool 消息的格式追加到对话历史。

工具执行失败时，错误信息也按同样的格式返回。模型看到错误后可以决定重试、换一个工具、或者直接告诉用户哪里出了问题。

### 06、短期记忆和长期记忆分别怎么实现？

短期记忆就是当前会话的对话历史和工具调用轨迹。

存在内存里，会话结束就清了。PaiCLI 保留最近 N 轮对话不做压缩，超出的部分用 LLM 摘要。派聪明用 Redis 存最近 20 条消息（key 是 conversation:{conversationId}，7 天过期），同时在 MySQL 永久保存完整记录。

【截图：短期记忆和长期记忆的存储架构；风格：three-layer；截图目标：展示两种记忆的存储位置、生命周期和检索方式差异；关键词：对话历史、持久化存储、检索注入】

长期记忆是跨会话持久化保存的信息，需要配合检索机制才能用起来。

PaiCLI 的长期记忆存在 JSON 文件里，每轮 LLM 调用前，记忆检索模块根据用户输入查询相关记忆，注入到 system prompt 中。项目级的记忆存在 PAI.md 文件里，按文件优先级叠加加载。

派聪明的会话记忆是双层设计：Redis 做热数据缓存（7 天过期），MySQL 做冷数据持久化（含完整的引用溯源信息——每条回答引用了哪个文档、第几页、相似度得分多少）。用户反馈也做长期保存，最近 5 条反馈注入到 system prompt 里，模型下次回答时会参考。

#### 哪些信息适合长期保存？

用户偏好（代码风格、语言习惯、审批策略）、历史反馈、项目上下文（技术栈、目录结构、团队约定）、高频使用的提示词模板——这些是跨会话都用得上的信息。

【截图：适合和不适合长期保存的信息分类；风格：checklist-card；截图目标：帮助读者快速判断哪些信息值得持久化；关键词：用户偏好、项目上下文、临时数据】

不适合长期保存的：临时的调试中间结果、一次性的工具调用输出、已经完成的任务指令。判断标准就一个——这条信息在下一次会话中还用得上吗？用不上就别存，存多了检索反而慢。

### 07、对话内容超过上下文窗口限制时怎么处理？

两种主流方案。

滑动窗口是最简单的做法：截断最早的对话，只保留最近 N 轮。实现简单，延迟几乎为零。缺点也明显——早期对话里可能有关键上下文，比如用户在第一轮说了项目背景，截断后模型直接“失忆”，后续回答可能牛头不对马嘴。

【截图：滑动窗口和动态摘要对比；风格：data-board；截图目标：展示两种方案的优缺点和适用场景差异；关键词：滑动窗口、动态摘要、信息保留率】

动态摘要是用 LLM 把历史对话压缩成一段摘要，替换原始消息。保留了重点信息，上下文利用率高。但摘要本身要调一次 LLM，增加延迟和成本；而且摘要质量不稳定，可能丢掉一些看起来不重要但后续会用到的细节。

PaiCLI 用的是两层压缩方案。

第一层压缩短期记忆。短期记忆条目的 token 数超过预算时，用 Map-Reduce 策略做 LLM 摘要——先把记忆分批压缩，再合并成一段总摘要。保留最近 N 轮不压缩，确保当前上下文的连贯性。

【截图：PaiCLI 两层压缩流程；风格：swimlane；截图目标：展示两层压缩的触发条件、压缩对象和保留策略；关键词：Map-Reduce 摘要、167k 触发阈值、保留 3 轮】

第二层压缩对话历史。每轮 LLM 调用前检查当前 token 数是否超过阈值——200k 窗口大约 167k 触发。超过了就启动压缩：找到所有 user 消息的位置，保留最近 3 轮完整交互（包含 tool_call 和 tool_result 的配对，不能切断），历史部分用 LLM 摘要替换。

摘要保留四类信息：关键诉求、已完成操作、达成共识、待办事项。分割点必须在 user 消息边界，不能切断工具调用和结果的配对。

### 08、多模态 Agent 中视觉编码器怎么和语言模型融合？

分三步。

第一步是视觉编码。图像先被切成固定大小的 patch（比如 14×14 像素），每个 patch 经过视觉编码器（通常是 ViT，也就是 Vision Transformer）提取特征，输出一组视觉特征向量。一张 224×224 的图片会被切成 256 个 patch，每个 patch 生成一个特征向量。

【截图：视觉编码器到 LLM 的融合流程；风格：swimlane；截图目标：展示图像从 patch 切分到最终 LLM 推理的完整数据流；关键词：ViT 编码、投影层、序列拼接】

第二步是投影层做维度转换。视觉特征向量的维度和文本 token 的 Embedding 维度通常不一样，需要一个投影层把两者映射到同一个向量空间。简单的做法是一个线性投影矩阵，一次矩阵乘法就完成维度转换。效果更好的是用 MLP（两层全连接加激活函数），多一层非线性变换，投影后的视觉向量和文本 Embedding 匹配得更好。

第三步是拼接送入 LLM。投影后的视觉 token 和文本 token 拼成一个统一的序列，送入 LLM。模型通过自注意力机制同时关注图像信息和文字信息——某个文本 token 在计算注意力时可以“看到”所有的视觉 token，把图像内容融入到文字推理中。

不同模型的融合深度不一样。LLaVA 用的是最直接的方案：线性投影加序列拼接，结构简单但效果已经很好。Qwen-VL 在此基础上加了交叉注意力（Cross-Attention），让文本 token 更主动地去“查询”图像特征，融合更深，对细粒度的图像理解（比如读图表里的数字）效果更好。

对 Agent 来说，多模态能力意味着工具的输入输出不再局限于文字。截图工具返回的图片、摄像头采集的画面，模型都可以直接“看懂”，然后基于视觉信息决定下一步操作。

### 09、SFT、强化学习和 Agentic CFT 分别解决什么问题？

三个阶段解决的问题不同。

SFT（Supervised Fine-Tuning，监督微调）解决的是“会不会”的问题。给模型一批正确的 Agent 调用轨迹——输入什么 prompt，调了什么工具，传了什么参数，最终输出什么——模型通过模仿学习掌握基本的工具调用格式和流程。SFT 之后，模型至少能生成格式正确的 tool_call，知道什么时候该调工具、什么时候该直接回答。

【截图：SFT/RL/Agentic CFT 三阶段训练流程；风格：three-layer；截图目标：展示三个阶段解决的问题和训练方式差异；关键词：监督微调、强化学习、Agentic CFT】

强化学习（RL）解决的是“好不好”的问题。SFT 教会了基本动作，但模型不一定知道哪种调用策略更优。RL 用奖励模型对不同的调用轨迹打分，引导模型学习更高效的决策路径。比如同一个任务，一条轨迹用了 3 步完成、另一条用了 8 步，奖励模型给前者打更高分。GRPO（Group Relative Policy Optimization，组相对策略优化）比 PPO 更适合 LLM 场景——直接在一组候选轨迹中用相对排名计算奖励，不需要额外训练一个 Critic 网络。

Agentic CFT（Completion Fine-Tuning）解决的是“真实环境里行不行”的问题。SFT 和 RL 都是在离线数据上训练的，模型的决策不会真的改变环境状态。Agentic CFT 让模型在真实的 Agent 环境里跑任务——调用真实的工具、拿到真实的返回结果、根据最终任务完成度给奖励。模型的每一步决策直接影响后续的环境状态，训练的是完整的多步决策过程。

#### 为什么要对 Observation Token 做 Mask？

Observation 是环境返回的内容——搜索结果、API 响应、文件内容——不是模型自己生成的。

训练时如果对 Observation Token 计算损失，模型会尝试“预测环境会返回什么”。这个任务既不合理（环境输出取决于外部系统，模型没法控制），也会干扰梯度信号——本来应该引导模型学习决策能力的梯度，被“预测环境输出”这个噪声稀释了。

【截图：Observation Token Mask 示意图；风格：whiteboard；截图目标：展示训练时哪些 token 计算 loss、哪些被 mask；关键词：Thought 计算 loss、Action 计算 loss、Observation 被 mask】

Mask 之后，损失只在 Thought Token 和 Action Token 上计算。模型的学习目标变得纯粹——怎么思考当前情况、怎么决定下一步行动。Observation 只作为上下文输入提供信息，不参与梯度更新。

### 10、Redis 在 Agent 系统中可以承担哪些作用？

四个典型场景。

第一个是会话状态缓存。用户的对话历史是热数据，每轮请求都要读。放在数据库里每次查延迟高。派聪明的做法是 Redis 存热对话——key 是 conversation:{conversationId}，JSON 数组格式，最多 20 条消息，7 天过期——MySQL 存全量历史做持久化。读写都走 Redis，MySQL 只在需要翻历史记录时查。

【截图：Redis 四个典型场景；风格：skill-card；截图目标：展示四种场景的 key 设计和过期策略；关键词：会话缓存、语义缓存、分布式锁、速率限制】

第二个是语义缓存。用户问过的问题，语义相近的再来一遍时不需要重新调 LLM。做法是把用户查询做 Embedding，在 Redis 里检索余弦相似度超过阈值的历史问答对。命中了直接返回缓存结果，省掉一次 LLM 调用。对高频重复问题（比如“怎么部署”“怎么登录”），效果很明显。

第三个是分布式锁。Agent 调用外部工具时，有些操作不能并发执行——比如同一个用户的文件写入操作，或者有副作用的 API 调用。Redis 的 SET NX 加过期时间可以做轻量级分布式锁，防止同一个用户并发触发同一个工具产生冲突。

第四个是速率限制。Agent 系统的 LLM 调用和 Embedding 调用都有配额上限。Redis 的 INCR 加 EXPIRE 做滑动窗口限流，比如每分钟 30 次聊天请求、每天 2000 次 Embedding 批量请求。派聪明的速率限制就是这么实现的。

过期策略要按场景分开设计：

- 会话状态：7 天过期，从最后一次交互开始计算，活跃会话持续续期，冷会话自动清理
- 语义缓存：热门问题长保留，命中次数越多过期时间越长；冷门问题短过期，比如 1 小时
- 分布式锁：秒级短过期，比如 30 秒，防止锁持有者异常退出导致死锁
- 速率限制：记得设一个过期时间和窗口周期一致，分钟级限流就 60 秒过期

### PaiCLI 如何写到简历上？

**项目名称**：PaiCLI — 终端 AI Agent 命令行工具

**项目简介**：对标 Claude Code 的 Java 版终端 Agent，支持 ReAct、Plan-and-Execute、Multi-Agent Team 三种执行模式，具备多轮对话、代码搜索、工具调用、自动修复等能力。

**技术栈**：Java 21 + Spring AI + LangGraph4j + Elasticsearch + Redis + MCP 协议

【截图：PaiCLI 简历技术栈一览；风格：skill-card；截图目标：展示核心技术栈和对应模块；关键词：Spring AI、LangGraph4j、MCP 协议】

**核心职责**：

- 设计 Agent 多模式架构，支持 ReAct 快速响应、Plan-and-Execute 复杂任务分步执行、Multi-Agent Team 多角色协作三种执行路径，根据任务复杂度自动路由
- 构建 9 层 Prompt 组装器，静态层前置命中 Prompt Caching，动态层按 token 预算裁剪，单次请求 token 成本降低约 35%
- 实现两层上下文压缩方案，短期记忆用 Map-Reduce 摘要，对话历史在接近窗口上限时整体压缩并保留最近 3 轮完整交互，支撑长会话不丢关键上下文
- 设计 Skills 渐进式加载体系，索引层常驻上下文（4KB 以内），正文和参考文档按需加载，LRU 缓冲区控制激活数量上限 3 个
- 搭建三层效果评估体系，维护 123 条确定性测试用例的 Golden Set，结合 LLM-as-Judge 批量评分和人工校准抽检，保障迭代不回退

## ending

这 10 道题里的 Tool Message 设计、Observation Mask、Redis 语义缓存这些点，有项目经验的候选人才答得踏实。动手搭一个 Agent 项目跑起来，比背十篇面经管用。

下期见。
